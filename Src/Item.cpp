#include "Item.h"
/*Item::Item(string t, int v):type(t),value(v)

Item::Item(string t, int v)
{
    type = "Default";
    value = 0;
}

Item::~Item()
{
}

bool heal
bool value
string type = "heal"

Item (string t,)

void Item::Item(Personnage &cible)
{
    if(type == "heal")
    {
        cible.setHealth(cible.getHealth()+value);
    }
    if(type == "damage")
    {
        cible.setHealth(cible.getHealth()-value);
    }
    if(type == "buff")
    {
        cible.setAttack(cible.getAttack()+value);
    }
}

void Item::showInfo()
{
    cout<<"type = "<<gettype<<endl;
}*/

/*

*/
